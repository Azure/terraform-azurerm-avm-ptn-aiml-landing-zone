# Standalone example

This example demonstrates a configuration when the platform landing zone flag is set to false. In this case, all supporting services are included as part of AI landing zone deployment.

The example opts the current deployment principal into the App Configuration Data Owner role through `security_definition`. Workload access remains least privilege and must be enabled per managed identity.
